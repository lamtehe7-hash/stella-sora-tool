# Python
__pycache__/
*.pyc
venv/

# Build output
build/
dist/

# Runtime (tự tạo khi chạy: log xoay vòng, config cá nhân serial/adb)
log/
config/

# Ảnh chụp khảo sát / test cục bộ (có thể lộ thông tin tài khoản game)
screenshots/
Img_test/

# Clone tham khảo AzurLaneAutoScript (repo GPL riêng, không thuộc project này)
reference/

# Claude Code local
.claude/settings.local.json
# Project settings có hook đường dẫn tuyệt đối máy cá nhân — không đẩy lên github
.claude/settings.json

# Kế hoạch / bàn giao nội bộ — không đẩy lên github
PLAN.md
PROJECT_TASK.md
HANDOVER.md

# Gói phát hành đóng cục bộ (zip lớn — tải lên GitHub Releases thủ công)
releases/

# Data capture cho nghiên cứu (ảnh + log, KHÔNG đẩy lên GitHub)
/data/ascension_capture/
/data/heartlink_survey/
